# Shared Notes

This folder stores cross-cutting contract notes for the MVP.

- `config/` keeps the airport, currency, and mock-flight data that both sides rely on conceptually.
- The backend response model is mirrored in `frontend/src/lib/types.ts`.
- Future versions can move JSON schemas or generated client types here.
